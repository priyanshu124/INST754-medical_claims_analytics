version: 2

models:
  - name: dim_patients
    description: Patient dimension containing demographic attributes
    columns:

      - name: patient_id
        tests:
          - not_null
          - unique
